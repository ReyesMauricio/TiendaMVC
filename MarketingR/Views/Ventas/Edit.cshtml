@model MarketingR.Models.Venta

@{
    ViewBag.Title = "Edit";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="row">
        <!-- /.col -->
        <div class="col-md-12">
            <div class="card card-outline card-success">
                <div class="card-header">
                    <h3 class="card-title">Editar venta</h3>

                    <div class="card-tools">
                        <button type="button" class="btn btn-tool" data-card-widget="collapse">
                            <i class="fas fa-minus"></i>
                        </button>
                    </div>
                    <!-- /.card-tools -->
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="row">
                        @Html.HiddenFor(model => model.IdVenta)

                        <div class="col-lg-6 col-sm-12 mb-3">
                            <label class="control-label">Cliente</label>
                            @Html.DropDownList("IdCliente", null, htmlAttributes: new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.IdCliente, "", new { @class = "text-danger" })
                        </div>

                        <div class="col-lg-6 col-sm-12 mb-3">
                            <label class="control-label">Fecha venta</label>
                            <input type="text" name="FechaVenta" id="FechaVenta" value="@ViewBag.FechaVenta" class="form-control" required />
                        </div>

                        <div class="col-lg-6 col-sm-12 mb-3">
                            @Html.LabelFor(model => model.Impuesto, htmlAttributes: new { @class = "control-label" })
                            @Html.EditorFor(model => model.Impuesto, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(model => model.Impuesto, "", new { @class = "text-danger" })
                        </div>

                        <div class="col-lg-6 col-sm-12 mb-3">
                            <label class="control-label">Total venta</label>
                            @Html.EditorFor(model => model.TotalVenta, new { htmlAttributes = new { @class = "form-control" } })
                            @Html.ValidationMessageFor(model => model.TotalVenta, "", new { @class = "text-danger" })
                        </div>
                    </div>
                </div>
                <!-- /.card-body -->
                <div class="card-footer">
                    <input type="submit" value="Editar" class="btn btn-success" />
                </div>
            </div>
            <!-- /.card -->
            <div class="mb-3">
                <button type="button" class="btn btn-primary" id="btnRegresar" name="btnRegresar" onclick="regresar()">
                    <i class="fas fa-arrow-alt-circle-left"></i> Regresar
                </button>
            </div>
        </div>
        <!-- /.col -->
    </div>
}


<script>
    function regresar() {
        document.getElementById("btnRegresar")
        location.href = "/Ventas/Index";
    }
</script>
